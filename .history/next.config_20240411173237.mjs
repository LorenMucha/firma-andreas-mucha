/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "https://lorenmucha.github.io/firma-andreas-mucha",
    output: "build",
    reactStrictMode: true,
};

export default nextConfig;
