/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export",
    distDir: 'out',
    basePath: "/firma-andreas-mucha",
    // basePath: "/firma-andreas-mucha",
};

export default nextConfig;
